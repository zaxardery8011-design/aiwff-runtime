---
name: Bug 回報
about: 回報 aiwff-runtime 的可重現錯誤
title: "[Bug] "
labels: bug
assignees: ""
---

## 問題摘要

請用一兩句話描述你遇到的錯誤。

## Worker 模式

- [ ] mock worker（`MOCK_WORKER=1`）
- [ ] 真 Claude worker（`ENABLE_REAL_CLAUDE_WORKER=1` 且 `MOCK_WORKER=0` 或留空）

## 環境

- 作業系統：
- Node.js 版本（`node --version`）：
- npm 版本（`npm --version`）：
- 啟動指令（例如 `npm run web`）：

## 重現步驟

1.
2.
3.

## 預期結果

請描述你原本預期會發生什麼。

## 實際結果

請描述實際發生什麼；若有錯誤畫面，也可附截圖。

## Log 片段

請貼上相關 log 片段。請勿貼出 token、chat id、帳號資訊或其他 secrets。

```text

```

## 相關檔案或 artifact

- task id：
- artifact 路徑（如適用）：
