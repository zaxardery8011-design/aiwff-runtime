# Runtime environment variables
# Copy this file to .env — npm install is optional (zero external dependencies)

PORT=3100

# Telegram Bot（選填，不填就只有 WebUI）
TG_BOT_TOKEN=
ADMIN_TG_CHAT_ID=

# Claude CLI（選填，不填走 mock）
CLAUDE_CMD=claude
MOCK_WORKER=
