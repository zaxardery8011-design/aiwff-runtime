{
  "_note": "ROADMAP ONLY — this file is not read by the current runtime. Only process.env.PORT is consumed. Future phases will wire up data_dir, worker.command, and mock_mode.",
  "port": 3100,
  "data_dir": "data",
  "worker": {
    "command": "node",
    "args": ["examples/mock-worker/worker.js"]
  },
  "mock_mode": true
}
